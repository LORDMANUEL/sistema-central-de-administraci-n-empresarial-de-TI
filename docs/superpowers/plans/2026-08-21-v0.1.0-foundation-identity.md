# v0.1.0 Foundation + Identity Service Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an installable repository foundation and a complete Identity Service baseline that can bootstrap the first administrator, authenticate, refresh sessions, authorize roles and manage users.

**Architecture:** `identity-service` is an independently deployable FastAPI service with SQLAlchemy-owned persistence and versioned API contracts. Tests use SQLite fixtures while deployment uses PostgreSQL through `DATABASE_URL`; no other service writes Identity tables.

**Tech Stack:** Python 3.12, FastAPI, Pydantic v2, SQLAlchemy 2.x, Argon2, PyJWT, pytest, Docker/Compose, PostgreSQL 16.

**Spec:** `docs/superpowers/specs/2026-08-21-it-guardian-platform-design.md`

## Global Constraints
- Microservices from v0.1.0; no cross-service database writes.
- Python service target is 3.12.
- All behavior changes follow TDD: failing test must be observed before production implementation.
- API is versioned under `/api/v1`.
- Passwords are Argon2 hashes and are never logged or returned.
- Access and refresh JWTs have distinct token types.
- Disabled users cannot log in or refresh.
- Production secrets come from environment variables.
- Service includes live/readiness checks, Dockerfile, tests and documentation before DONE.

---

### Task 1: Repository foundation and service contract
**Files:** `MASTER.md`, `VERSION`, `.gitignore`, `.env.example`, design spec, service README and `pyproject.toml`.
**Interfaces:** produces the documented service boundary and dependency manifest.
- [ ] Add project/version/config documentation matching the approved design.
- [ ] Add FastAPI, uvicorn, SQLAlchemy, psycopg, pydantic-settings, argon2-cffi, PyJWT, pytest and httpx dependencies.
- [ ] Parse TOML with `tomllib`.
- [ ] Commit foundation files.

### Task 2: Configuration, database and health — TDD
**Files:** `tests/test_health.py`, `app/config.py`, `app/database.py`, `app/main.py`, `app/__init__.py`.
**Interfaces:** produces `create_app() -> FastAPI`, `get_settings() -> Settings`, `get_db()`.
- [ ] Write failing health tests.
- [ ] Run tests and observe RED.
- [ ] Implement minimal settings/database/app/health endpoints.
- [ ] Re-run and observe GREEN.
- [ ] Run full suite and commit.

### Task 3: User model, roles and bootstrap — TDD
**Files:** `tests/test_bootstrap.py`, `app/models.py`, `app/schemas.py`, `app/security.py`, `app/api.py`.
**Interfaces:** produces `User`, `Role`, password hashing and bootstrap API.
- [ ] Write failing bootstrap tests: first success, second 409, no password hash in response.
- [ ] Observe RED.
- [ ] Implement model, Argon2 and endpoint.
- [ ] Observe GREEN and commit.

### Task 4: Login and refresh tokens — TDD
**Files:** `tests/test_auth.py`, `app/security.py`, `app/api.py`, `app/schemas.py`.
**Interfaces:** produces access/refresh token functions and auth endpoints.
- [ ] Write failing tests for login, wrong password, refresh, token-type rejection and disabled user.
- [ ] Observe RED.
- [ ] Implement JWT claims and endpoints.
- [ ] Observe GREEN and commit.

### Task 5: Current user and user administration — TDD
**Files:** `tests/test_users.py`, `app/api.py`, `app/schemas.py`, `app/security.py`.
**Interfaces:** produces bearer dependency, role guard and user administration endpoints.
- [ ] Write failing tests for `/users/me`, create/list/disable, duplicate email and non-admin rejection.
- [ ] Observe RED.
- [ ] Implement auth dependency/guard/endpoints.
- [ ] Observe GREEN and commit.

### Task 6: Containerization and installable foundation
**Files:** service Dockerfile/.dockerignore, `compose.yaml`, service README.
- [ ] Add non-root Python 3.12-slim image.
- [ ] Add PostgreSQL 16 and identity service to Compose.
- [ ] Validate Compose when Docker is available; otherwise parse configuration and record limitation.
- [ ] Document bootstrap/login/run commands and commit.

### Task 7: CI and release verification
**Files:** `.github/workflows/identity-ci.yml`, `CHANGELOG.md`.
- [ ] Run Python 3.12 dependency install and pytest in GitHub Actions.
- [ ] Add `compileall` verification.
- [ ] Run local tests final time.
- [ ] Update changelog.
- [ ] Commit and open draft PR to `main`.
