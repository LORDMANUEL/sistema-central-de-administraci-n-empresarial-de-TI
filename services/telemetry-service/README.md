# Telemetry Service

Bounded, idempotent IT Guardian v0.6 endpoint telemetry. Only the documented metric/label vocabulary is accepted; arbitrary tags and payload blobs are rejected. Latest reads are administrative and tenant-scoped through the existing Asset authorization boundary.
