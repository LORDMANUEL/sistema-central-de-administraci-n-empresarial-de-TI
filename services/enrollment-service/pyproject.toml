[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "it-guardian-enrollment-service"
version = "0.4.0"
description = "IT Guardian secure endpoint enrollment microservice"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.128,<1",
  "uvicorn[standard]>=0.35,<1",
  "sqlalchemy>=2.0,<3",
  "psycopg[binary]>=3.2,<4",
  "pydantic-settings>=2.10,<3",
  "PyJWT>=2.10,<3",
  "cryptography>=45,<47",
  "httpx>=0.28,<1",
  "prometheus-client>=0.22,<1",
  "alembic>=1.16,<2",
  "nats-py>=2.10,<3",
]

[project.optional-dependencies]
test = ["pytest>=9,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.hatch.build.targets.wheel]
packages = ["app"]
