# Command Service

Durable, allowlisted endpoint jobs for IT Guardian v0.6. Administrative target validation is performed against Asset and Agent Control; device acquisition/results require trusted certificate-derived device context. Arbitrary shell, PowerShell, executable paths, URLs and scripts are intentionally unsupported in v0.6.
